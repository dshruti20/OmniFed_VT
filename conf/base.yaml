# ================================================================================
# Base Configuration
#
# Main configuration template with required components for federated learning experiments.
# This is the primary config that composes all other components.
# ================================================================================

defaults:
  - topology: base
  - algorithm: base
  - model: base
  - datamodule: base
  - _self_

# ─────────────────────────────────────────
# Engine / Experiment Config
# ─────────────────────────────────────────

# Number of federated learning rounds (sets of epochs) to execute
global_rounds: ???

# Whether to allow overwriting existing experiment output directories
overwrite: false

# ─────────────────────────────────────────
# Ray Cluster Configuration
# ─────────────────────────────────────────
# Controls distributed compute infrastructure for federated learning
# Docs: https://docs.ray.io/en/latest/ray-core/api/doc/ray.init.html

ray:
  # ─────────────────────────────────────────
  # Cluster Connection & Resource Allocation

  # Cluster connection (null = auto-detect local cluster)
  # Use "ray://host:port" for remote clusters, "local" to force local
  address: "local"

  # Resource allocation - CRITICAL for proper GPU/CPU distribution
  # null = auto-detect based on hardware, explicit numbers override detection
  num_cpus: null
  num_gpus: null
  resources: {} # Custom resources: {"accelerator_type": "V100", "high_memory": 2}

  # GPU allocation: Automatic (shares GPUs when needed, 1 per actor otherwise)
  # Override when needed: ray_actor_options.num_gpus in topology configs
  # Debug: Check [GPU-AUTO] logs during startup

  # ─────────────────────────────────────────
  # Memory & Performance

  # Object store memory for large model sharing (null = 30% of system memory)
  object_store_memory: null

  # ─────────────────────────────────────────
  # Monitoring & Development

  # Essential for FL: forward all distributed node logs to main process
  log_to_driver: true

  # Ray dashboard (null = auto-start if dependencies available)
  include_dashboard: null
  dashboard_host: "127.0.0.1" # Use "0.0.0.0" for external access
  dashboard_port: null # null = auto-find port starting from 8265

  # Development convenience - allow multiple ray.init() calls without error
  ignore_reinit_error: true

  # ─────────────────────────────────────────
  # Advanced Configuration

  # Experiment isolation (null = anonymous namespace)
  namespace: null

  # Runtime environment for distributed workers (empty = inherit from main process)
  runtime_env: {} # Example: {"pip": ["torch==1.12.0"], "env_vars": {"CUDA_VISIBLE_DEVICES": "0,1"}}

  # Commented advanced options (uncomment if needed):
  # object_spilling_directory: null    # Directory for object spilling (null = system temp)
  # labels: {}                         # [Experimental] Node labels: {"node_type": "gpu_worker"}
  # configure_logging: true            # Whether Ray can configure logging
  # logging_level: null                # Ray logging level (null = INFO)
  # enable_resource_isolation: false   # Use cgroups for resource isolation

# ─────────────────────────────────────────
# Hydra Configuration
# ─────────────────────────────────────────
# Docs:
# - https://hydra.cc/docs/configure_hydra/intro/
# - https://hydra.cc/docs/configure_hydra/job/

hydra:
  job:
    env_set:
      # Environment variables for the Hydra job
      # These can be used to configure the runtime environment for FL workers.
      # Note: These variables will be copied to all FL workers

      OMNIFED_ENV_DUMMY1: "dummy_value1" # Example variable for demonstration

      # Example settings:

      # GPU & threading:
      # CUDA_VISIBLE_DEVICES: "0,1"        # Assign specific GPUs
      # OMP_NUM_THREADS: 4                 # Limit CPU threads per client

      # PyTorch debugging:
      # NCCL_DEBUG: INFO                   # GPU communication debug
      # TORCH_DISTRIBUTED_DEBUG: DETAIL    # Distributed training debug
      # TORCH_CPP_LOG_LEVEL: INFO          # PyTorch C++ logs

      # Framework logging:
      # TF_CPP_MIN_LOG_LEVEL: 3            # Suppress TensorFlow warnings
      # GRPC_VERBOSITY: ERROR              # Minimize gRPC logging

    # Copy local environment variables to FL workers:
    # env_copy:
    #   - WANDB_API_KEY # W&B experiment tracking

  run:
    # Output directory for FL experiment results and logs
    # Docs: https://hydra.cc/docs/configure_hydra/workdir/
    dir: outputs/${now:%Y-%m-%d_%H-%M-%S}/${hydra.job.config_name}

    # Alternative example patterns:
    # dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}              # Group by date/time
    # dir: outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}   # Group by job name
    # dir: outputs/${algorithm._target_}/${now:%Y-%m-%d}        # Group by algorithm

# ─────────────────────────────────────────
# Slurm Cluster Configuration
# ─────────────────────────────────────────
# Controls distributed compute infrastructure for federated learning
# Docs: https://slurm.schedmd.com/overview.html

slurm:
  # Hybrid (``engine.communication_mode=hybrid``): Engine sets ``#SBATCH --ntasks=W`` where
  # ``W`` = hybrid ``world_size``. Choose ``nodes × ntasks_per_node >= W`` (``docs/archive/hybrid-engine-pipeline/HYBRID_SLURM_REFERENCE.md`` §4.3);
  # if ``nodes`` is too small, Engine may raise it and log ``[Engine] slurm.nodes raised …``.
  enabled: false
  account: null
  partition: debug
  qos: null
  time: "02:00:00"
  nodes: 2
  ntasks_per_node: 1
  cpus_per_task: 8
  gpus_per_node: 1
  gpus_per_task: 1
  gres: gpu:1
  job_name: "omnifed"
  # When true, emitted as ``#SBATCH --exclusive`` (whole-node allocation; Frontier ``batch`` is often exclusive already).
  exclusive: false
  constraint: null
  reservation: null
  setup_lines:
    # site modules/env (edit for Frontier/site)
    - 'module load cray-python || true'
    - 'export OMP_NUM_THREADS=8'
  # Parent Lustre dir for hybrid round checkpoints; use with ``experiment_id``.
  checkpoint_dir: null
  # Subdirectory under ``checkpoint_dir`` (stable across resubmits). See ``docs/CHECKPOINTING_HYBRID_SLURM_PLAN.md``.
  experiment_id: null
  # When true, load ``manifest.json`` + latest round weights and continue from ``next_round_idx``.
  resume: false
  # Emit ``#SBATCH -d singleton`` (one chain job at a time). Use with ``scripts/hybrid_checkpoint_autochain.sh``.
  dependency_singleton: false
  preempt_signal: "USR1"
  preempt_notice_sec: 180
  # Legacy / unused for hybrid round ckpt (use ``resume`` + ``checkpoint_dir`` + ``experiment_id``).
  resume_from: null


engine:
  mode: ray   # one of: ray, slurm
  # Slurm-only (Phase B): classic = one process group (TorchDist); hybrid = conf_hybrid layout + step 7 worker.
  communication_mode: classic
  hybrid:
    # File under conf_hybrid/topology/*.yaml OR leave null when using runtime ``layout`` below.
    topology_config: null
    # Runtime topology: kwargs for ``build_hybrid_topology`` (omit or null to use ``topology_config`` only).
    # Optional nested ``communicators:`` merges labels onto topology_builder.DEFAULT_HYBRID_COMMUNICATORS (declarative; wiring unchanged).
    # If both are set, ``layout`` wins. Example symmetric 2×3 + RPC: num_facilities: 2, mpi_ranks_per_facility: 3, dedicated_rpc_server: true (plus rpc_port / facility_mpi_* as needed).
    layout: null
    # Optional; merged onto hybrid cfg ``training`` (e.g. dataset_total_clients) when using ``layout``.
    training: null
    # Hybrid Slurm RPC server lifetime (see docs/archive/hybrid-engine-pipeline/HYBRID_SLURM_REFERENCE.md).
    server_run_extra_sec: 120
    server_sec_per_round: 300
    # sleep = fixed heuristic; leader_done = wait until each gRPC leader rank touches a marker (max wait below).
    server_shutdown: leader_done
    leader_done_poll_sec: 5.0
    # What local MPI and global gRPC aggregate: params (FedAvg) or gradients (FedSGD).
    aggregate_payload: params   # params | gradients
    # Global (cross-facility) hybrid gRPC compression — facility TorchMPI stays dense.
    global_compression:
      enabled: false
      scheme: topk   # topk | qsgd
      compress_ratio: 0.01
      bit_width: 8   # qsgd only: s where levels = 2^s
  classic:
    aggregate_payload: params   # params | gradients
